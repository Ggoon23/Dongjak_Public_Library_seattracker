name: djlib-seat-server
runtime: python
version: "3.12"
rootDirectory: server
buildCommand: pip install -r requirements.txt
startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 30
port: 3000
